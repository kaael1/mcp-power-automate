{
  "name": "mcp-power-automate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local MCP server plus browser extension bridge for editing the current Power Automate flow.",
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  }
}
